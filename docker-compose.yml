version: '3.7'

services:

    node:
        container_name: 'thiagoalves.dev'
        build: .docker/node
        volumes:
            - '.:/var/www/html/thiagoalves.dev'
        ports:
            - '1000:1000'
        command: >
            bash -c "yarn install && yarn dev"
